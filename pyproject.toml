[project]
name = "train-status"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "confluent-kafka>=2.13.2",
    "dotenv>=0.9.9",
    "pydantic>=2.12.5",
]
